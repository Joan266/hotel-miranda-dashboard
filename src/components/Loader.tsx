import React from "react";
import { Loader, Loading } from "../styles/loader";

export const LoaderComponent = () => {
    return (
        <Loading>
            <Loader />
        </Loading>
    );
}
